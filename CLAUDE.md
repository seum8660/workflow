# 프로젝트 규칙

## 요약문서 추가 시 자동 반영
사용자가 새 요약문서(HTML)를 첨부하면 별도 지시가 없어도 다음을 모두 수행한다.
1. `manuals/`에 다음 번호로 복사
2. `data/wf_boards.js`에 해당 업무의 워크플로우 보드 추가(레인×단계 격자, 노드에 법령·기한·병목 포함) + `WF_ORDER`에 키 추가
3. `data/wf_tags.js`의 `WF_BOARDNM`·`WF_TAGS`·`WF_PHASE` 갱신
4. `data/wf_docs.js`에 문서 항목 추가(관련 보드 키 매핑)
5. `학교시설 워크플로우 보드.dc.html`의 `defaultBoard` enum에 키 추가
6. 인트로(`학교시설 워크플로우 인트로.dc.html`)와 전자책(`학교시설 워크플로우 전자책.dc.html`)의 보드 수·요약문서 수 문안 갱신
7. 보드 명칭은 한글 우선(영문 약어 단독 사용 금지)
